# The official Webpack utility depends on an opdev package whose postinstall
# copies bundled modules. Running lifecycle scripts serially avoids a race in
# newer npm versions.
foreground-scripts=true
